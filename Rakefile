namespace :addons do
  namespace :heroku do
    desc 'addons:heroku:push - push env vars to heroku'
    task :push do
      # backup .env to .env.original

      # copy addons.json to .env

      # run `heroku config:push`

      # replace .env with .env.original
    end
  end
end

